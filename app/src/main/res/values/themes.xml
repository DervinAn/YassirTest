<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App theme used after splash screen -->
    <style name="Theme.YassirTest" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash theme used temporarily at launch -->
    <style name="Theme.YassirTest.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_launcher_playstore</item>
    </style>

</resources>
